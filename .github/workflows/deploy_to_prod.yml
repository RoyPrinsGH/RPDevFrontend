name: royprins.dev CI/CD - Deploy to production

on:
  push:
    branches: [ "production" ]

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v3
    - name: LS
      run: ls
    - name: Check GH Actions secret access
      run: echo ${{ secrets.PRIVATE_KEY }}
